// var n = "asdsdasdsds"

// vow = ['a', 'e', 'i', 'o', 'u'];
// vowels = [];
// cons = [];
// for (let i = 0; i < n.length; i++ ){
//     if (vow.includes(n[i])){
//         vowels.push(n[i]);
//     } else {
//         cons.push(n[i]);
//     }
// }


// console.log(`The vowels are ${vowels} and the consonant ${cons}`);
// let arr = [1, 2, 3, 4];

// console.log(arr.join(" "));
// arr.pop()
// arr.shift()
// arr.unshift()
// arr.fill()

user = {
    name: [],
    age: []
}
for (const key in user) {
    console.log(user[key].push('Ready', "Boss"));      
    }

console.log(user.name.toString());